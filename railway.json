{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "playwright install --with-deps chromium && python server.py",
    "restartPolicyType": "always"
  }
}
